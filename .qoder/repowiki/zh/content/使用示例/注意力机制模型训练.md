<cite>
**Referenced Files in This Document**  
- [train.py](file://Attention/train.py)
- [attention.py](file://Attention/attention.py)
- [loss.py](file://Attention/loss.py)
- [dataloder_ATT.py](file://pre_process/dataloder_ATT.py)
</cite>

## 目录
1. [注意力机制模型训练](#注意力机制模型训练)
2. [项目结构](#项目结构)
3. [核心组件](#核心组件)
4. [训练流程详解](#训练流程详解)
5. [损失函数与权重策略](#损失函数与权重策略)
6. [训练监控与结果可视化](#训练监控与结果可视化)
7. [关键参数配置](#关键参数配置)
8. [常见问题与解决方案](#常见问题与解决方案)

## 项目结构

```mermaid
graph TD
subgraph "Attention"
A[attention.py]
B[loss.py]
C[train.py]
end
subgraph "pre_process"
D[dataloder_ATT.py]
E[SourceData.py]
F[TargetData.py]
G[data.py]
end
C --> A
C --> B
C --> D
```

**Diagram sources**  
- [train.py](file://Attention/train.py)
- [attention.py](file://Attention/attention.py)
- [loss.py](file://Attention/loss.py)
- [dataloder_ATT.py](file://pre_process/dataloder_ATT.py)

**Section sources**  
- [train.py](file://Attention/train.py)
- [pre_process/dataloder_ATT.py](file://pre_process/dataloder_ATT.py)

## 核心组件

### 交叉注意力模型结构

```mermaid
classDiagram
class CrossAttentionModel {
+feature_extractor : FeatureExtractor
+cross_attention : CrossAttentionModule
+classifier : Classifier
+forward(src_data, tgt_data)
}
class FeatureExtractor {
+conv1d_layers : Sequential
+conv2d_layers : Sequential
+fc : Sequential
+forward(x)
}
class CrossAttentionModule {
+attention : MultiHeadAttention
+q_s_linear : Linear
+k_s_linear : Linear
+v_s_linear : Linear
+q_t_linear : Linear
+k_t_linear : Linear
+v_t_linear : Linear
+q_c_linear : Linear
+k_c_linear : Linear
+v_c_linear : Linear
+forward(feat_s, feat_t)
}
class MultiHeadAttention {
+dim : int
+num_heads : int
+head_dim : int
+scale : float
+forward(q, k, v)
}
class Classifier {
+fc : Linear
+forward(x)
}
CrossAttentionModel --> FeatureExtractor : "包含"
CrossAttentionModel --> CrossAttentionModule : "包含"
CrossAttentionModel --> Classifier : "包含"
CrossAttentionModule --> MultiHeadAttention : "使用"
```

**Diagram sources**  
- [attention.py](file://Attention/attention.py#L203-L224)

**Section sources**  
- [attention.py](file://Attention/attention.py#L203-L224)

### 损失函数组件

```mermaid
classDiagram
class LossFunction {
+ce_loss : CrossEntropyLoss
+mse_loss : MSELoss
+alpha : float
+beta : float
+gamma : float
+delta : float
+source_loss(pred_s, labels_s)
+target_loss(pred_t, labels_t)
+cross_feature_loss(F_s, F_t)
+consistency_loss(F_s, F_c)
+adversarial_loss(F_s, F_t)
+total_loss(pred_s, pred_t, F_s, F_t, F_c, labels_s, labels_t)
}
class CrossEntropyLoss {
+forward(input, target)
}
class MSELoss {
+forward(input, target)
}
LossFunction --> CrossEntropyLoss : "使用"
LossFunction --> MSELoss : "使用"
```

**Diagram sources**  
- [loss.py](file://Attention/loss.py#L4-L77)

**Section sources**  
- [loss.py](file://Attention/loss.py#L4-L77)

## 训练流程详解

### 主训练流程

```mermaid
sequenceDiagram
participant Main as main()
participant Model as CrossAttentionModel
participant Optimizer as AdamW
participant Scheduler as CosineAnnealingLR
participant Train as train_epoch()
participant Test as test_model()
participant Save as save_best_model()
Main->>Model : 初始化模型
Main->>Main : 模型结构测试
Main->>Main : 初始化损失函数
Main->>Optimizer : 配置AdamW优化器
Main->>Scheduler : 配置余弦退火调度器
loop 每个训练轮次
Main->>Train : 调用train_epoch()
Train->>Model : 前向传播
Train->>Train : 计算各项损失
Train->>Train : 加权总损失
Train->>Train : 反向传播
Train->>Train : 梯度裁剪
Train->>Train : 参数更新
Train->>Scheduler : 更新学习率
Main->>Test : 调用test_model()
Test->>Model : 前向传播
Test->>Test : 计算测试损失和准确率
Main->>Save : 保存最佳模型
Main->>Main : 早停检查
Main->>Main : 动态调整损失权重
end
Main->>Main : 绘制训练曲线
Main->>Main : 保存结果
```

**Diagram sources**  
- [train.py](file://Attention/train.py#L149-L285)

**Section sources**  
- [train.py](file://Attention/train.py#L149-L285)

### 训练轮次执行流程

```mermaid
flowchart TD
Start([开始训练轮次]) --> DataLoad["加载源域和目标域数据"]
DataLoad --> BatchAlign["对齐批次大小"]
BatchAlign --> Forward["模型前向传播"]
Forward --> LossCalc["计算四项损失"]
LossCalc --> WeightedSum["加权总损失"]
WeightedSum --> Backward["反向传播"]
Backward --> GradClip["梯度裁剪 (max_norm=1.0)"]
GradClip --> Update["参数更新"]
Update --> Schedule["学习率调度"]
Schedule --> End([结束训练轮次])
```

**Diagram sources**  
- [train.py](file://Attention/train.py#L17-L102)

**Section sources**  
- [train.py](file://Attention/train.py#L17-L102)

## 损失函数与权重策略

### 损失函数类型

| 损失类型 | 计算方法 | 作用 |
|---------|--------|------|
| **源域分类损失** | 交叉熵损失 | 确保源域分类准确性 |
| **目标域分类损失** | 交叉熵损失 | 确保目标域分类准确性 |
| **跨域特征损失** | 余弦相似性损失 | 促进源域和目标域特征对齐 |
| **一致性损失** | L1损失 | 确保源域特征与交叉注意力特征一致性 |

**Section sources**  
- [loss.py](file://Attention/loss.py#L4-L77)

### 动态权重调整策略

```mermaid
flowchart TD
Start([训练开始]) --> Initial["初始权重: alpha=1.0, beta=1.0<br/>gamma=0.3, delta=0.2"]
Initial --> EpochCheck{"训练轮次 > 20?"}
EpochCheck --> |否| NoAdjust["保持初始权重"]
EpochCheck --> |是| Adjust["动态调整: gamma *= 0.98, delta *= 0.98"]
Adjust --> BoundCheck["边界检查: gamma >= 0.1, delta >= 0.05"]
BoundCheck --> Continue["继续训练"]
NoAdjust --> Continue
Continue --> End([训练结束])
```

**Section sources**  
- [train.py](file://Attention/train.py#L250-L253)

## 训练监控与结果可视化

### 训练输出监控

```mermaid
graph TD
subgraph "训练输出"
A["损失曲线"]
B["准确率曲线"]
C["学习率调度曲线"]
end
A --> A1["源域损失"]
A --> A2["目标域损失"]
A --> A3["跨域特征损失"]
A --> A4["一致性损失"]
B --> B1["测试准确率"]
C --> C1["学习率变化"]
subgraph "保存路径"
D["results/improved_training_curves.png"]
end
A --> D
B --> D
C --> D
```

**Section sources**  
- [train.py](file://Attention/train.py#L255-L285)

## 关键参数配置

### 核心训练参数

| 参数 | 值 | 设置依据 |
|------|----|---------|
| **batch_size** | 32 | 平衡内存使用和训练稳定性 |
| **学习率** | 3e-4 | AdamW优化器的推荐初始学习率 |
| **训练轮数** | 100 | 确保模型充分收敛 |
| **早停耐心值** | 15 | 防止过拟合的合理阈值 |
| **梯度裁剪** | max_norm=1.0 | 防止梯度爆炸 |

**Section sources**  
- [train.py](file://Attention/train.py#L149-L285)
- [dataloder_ATT.py](file://pre_process/dataloder_ATT.py#L34-L35)

### 模型超参数

| 组件 | 参数 | 值 |
|------|------|----|
| **特征提取器** | feature_dim | 512 |
| **多头注意力** | dim | 512 |
| **多头注意力** | num_heads | 8 |
| **分类器** | num_classes | 10 |
| **余弦退火** | T_max | 50 |
| **余弦退火** | eta_min | 1e-6 |

**Section sources**  
- [attention.py](file://Attention/attention.py#L203-L224)
- [train.py](file://Attention/train.py#L149-L285)

## 常见问题与解决方案

### CUDA内存不足解决方案

```mermaid
flowchart TD
Problem["CUDA内存不足"] --> Solution1["减小batch_size"]
Problem --> Solution2["使用梯度累积"]
Problem --> Solution3["启用混合精度训练"]
Problem --> Solution4["优化数据加载器"]
Problem --> Solution5["清理GPU缓存"]
Solution1 --> Detail1["将batch_size从32降至16或8"]
Solution2 --> Detail2["累积多个小批次的梯度再更新"]
Solution3 --> Detail3["使用torch.cuda.amp"]
Solution4 --> Detail4["设置dataloader的num_workers"]
Solution5 --> Detail5["调用torch.cuda.empty_cache()"]
```

**Section sources**  
- [train.py](file://Attention/train.py)
- [dataloder_ATT.py](file://pre_process/dataloder_ATT.py)